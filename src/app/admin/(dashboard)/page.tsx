export const metadata = { title: "Admin Dashboard | ADEOLA Global Ltd" };

export default function AdminDashboardPage() {
  return (
    <div>
      <h1 className="font-display text-display-l text-deep-plum">Dashboard</h1>
      <p className="mt-2 text-body-m text-muted-foreground">
        KPI overview lands in the next commit — this page confirms the session guard works.
      </p>
    </div>
  );
}
